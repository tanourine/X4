name: tanourine_app
description: تطبيق تانورين لإدارة الصيانة والفواتير والدوام
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/motivation_messages.json
